mcp>=1.0.0
requests>=2.28.0
azure-identity>=1.14.0

# Optional: NLP-based PII detection (Pass 2 safety net).
# Pass 2 is INACTIVE until you install these packages AND the spaCy model:
#
#   pip install presidio-analyzer presidio-anonymizer spacy
#   python -m spacy download en_core_web_sm
#
# Without them the server runs Pass 1 (registry lookup) only and warns on startup.
# presidio-analyzer>=2.2.0
# presidio-anonymizer>=2.2.0
# spacy>=3.5.0
